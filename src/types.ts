export type FormData = {
  purchase: string;
  card: string;
  store: string;
  amount: number;
  category: string;
  want_or_need: string;
  date: string;
  id?: number;
  notes?: string;
  flag?: boolean;
};
